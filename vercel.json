{
  "version": 2,
  "installCommand": "echo 'Skipping root install'",
  "buildCommand": "npm install --legacy-peer-deps --force && npm run build && npm install --prefix ../api",
  "outputDirectory": "build",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
